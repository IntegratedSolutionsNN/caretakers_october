scopes:

    enabled:

        # Filter name
        label: keios.prouser::lang.controllers.hide_disabled

        # Filter type
        type: checkbox

        # SQL Conditions
        conditions: is_enabled = 1